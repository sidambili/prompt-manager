# Supabase Configuration (Required)
# Get these from your Supabase project: Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Deployment Mode (Optional)
# Options: "cloud" (default), "self-hosted", "local"
# - "cloud": Presets for managed Supabase. Defaults OAuth to true.
# - "self-hosted": Presets for VPS/Custom infra. Defaults OAuth to false.
# - "local": Presets for local development. Defaults OAuth to false.
# NEXT_PUBLIC_DEPLOYMENT_MODE=cloud

# OAuth Provider Control (Optional)
# Overrides the defaults provided by NEXT_PUBLIC_DEPLOYMENT_MODE.
# NEXT_PUBLIC_ENABLE_OAUTH_GOOGLE=true
# NEXT_PUBLIC_ENABLE_OAUTH_GITHUB=true

# Server-side Admin Key (Optional, for advanced use cases)
# This key bypasses Row Level Security (RLS). NEVER expose to client-side code.
# SUPABASE_SERVICE_ROLE_KEY=

# Site URL for redirect base (Optional, for production deployments)
# Used to construct safe redirect URLs in auth callback
# NEXT_PUBLIC_SITE_URL=https://yourdomain.com

